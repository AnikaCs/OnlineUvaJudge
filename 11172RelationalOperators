#include<stdio.h>
int main()
{
	int num,i,n1,n2;
	scanf("%d",&num);
	for(i=1;i<=num;i++)
	{
		scanf("%d%d",&n1,&n2);
		if(n1==n2)
		     printf("=\n");
		if(n1>n2)
		      printf(">\n");
		if(n1<n2)
		    printf("<\n");	       
	}
	return 0;
}
